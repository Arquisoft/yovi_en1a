[[section-deployment-view]]
== Deployment View

=== Infrastructure Level 1: Azure Cloud Environment

The system is deployed on a single Azure Virtual Machine running Ubuntu. All components, including the frontend, are containerized to ensure consistency between development and production.

[plantuml, level1-whitebox, svg]
----
@startuml
skinparam backgroundColor #FAFAFA
skinparam node {
  BackgroundColor #D6EAF8
  BorderColor #2980B9
  FontStyle bold
}
skinparam component {
  BackgroundColor #EBF5FB
  BorderColor #2980B9
}
skinparam database {
  BackgroundColor #FEF9E7
  BorderColor #F39C12
}
skinparam ArrowColor #2980B9

node "Azure Cloud" {
  node "Azure Virtual Machine\n(Ubuntu 24.04 LTS)" as vm {

    component "webapp\n(Nginx + React/Vite)\n:80" as webapp
    component "users\n(Node.js REST API)\n:3000" as users
    component "gamey\n(Rust Game Engine)\n:4000" as gamey
    component "prometheus\n(Metrics Scraper)\n:9090" as prometheus
    component "grafana\n(Dashboards)\n:9091" as grafana
    database  "mongodb\n(Document Store)\n:27017" as mongodb

  }
}

actor "End User\n(Browser)" as user
actor "Admin / Ops" as admin

user  --> webapp      : HTTP :80
user  --> users       : HTTP :3000\n(API)
user  --> gamey       : HTTP :4000\n(Game moves)
admin --> prometheus  : HTTP :9090
admin --> grafana     : HTTP :9091

users --> mongodb     : read/write
gamey --> mongodb     : read/write

@enduml
----

=== Infrastructure Level 2: Container Orchestration (Docker Compose)

The following diagram illustrates the internal monitor-net bridge network and how the containers interact. Note that the Webapp is served via a container on port 80, and Grafana maps internal port 3000 to external 9091.

[plantuml, level2-docker, svg]
----
@startuml
skinparam backgroundColor #FAFAFA
skinparam rectangle {
  BackgroundColor #EBF5FB
  BorderColor #2980B9
  RoundCorner 12
}
skinparam database {
  BackgroundColor #FEF9E7
  BorderColor #F39C12
}
skinparam ArrowColor #2980B9
skinparam NoteBorderColor #F39C12
skinparam NoteBackgroundColor #FEF9E7

title Docker Compose — monitor-net bridge network

rectangle "monitor-net (bridge)" {

  rectangle "**webapp**\nghcr.io/arquisoft/yovi_en1a-webapp\nHost: 80 → Container: 80\nVITE_API_URL=http://localhost:3000\nVITE_GAMEY_URL=http://localhost:4000" as webapp

  rectangle "**users**\nghcr.io/arquisoft/yovi_en1a-users\nHost: 3000 → Container: 3000" as users

  rectangle "**gamey**\nghcr.io/arquisoft/yovi_en1a-gamey\nHost: 4000 → Container: 4000" as gamey

  rectangle "**mongodb**\nmongo:7\nHost: 27017 → Container: 27017" as mongodb

  rectangle "**prometheus**\nprom/prometheus\nHost: 9090 → Container: 9090" as prometheus

  rectangle "**grafana**\ngrafana/grafana\nHost: 9091 → Container: 3000" as grafana

}

database "Named Volume\nmongo-data\n→ /data/db" as mongovol
database "Bind Mount\n./users/monitoring/prometheus\n→ /etc/prometheus" as pmount
database "Bind Mount\n./users/monitoring/grafana/provisioning\n→ /etc/grafana/provisioning" as gmount

webapp     --> users      : depends_on\nREST :3000
webapp     --> gamey      : REST :4000
users      --> mongodb    : depends_on\nread/write :27017
gamey      --> mongodb    : depends_on\nread/write :27017
prometheus --> users      : scrape metrics\n(internal network)
prometheus --> gamey      : scrape metrics\n(internal network)
grafana    --> prometheus : query metrics\n:9090

mongodb .. mongovol
prometheus .. pmount
grafana    .. gmount

note right of grafana
  Internal port 3000 re-mapped
  to host port 9091 to avoid
  conflict with the users service
end note

note right of mongodb
  Port 27017 should NOT be
  exposed on production;
  only accessible inside
  monitor-net
end note

@enduml
----

=== Mapping of Building Blocks to Infrastructure

[cols="1,2,2"]
|===
|Building Block |Infrastructure Element |Description

|webapp
|Docker Container (Nginx)
|Serves the React/Vite frontend. Accessible via Port 80.

|users
|Docker Container (Node.js)
|Handles identity, history, and stats. Accessible via Port 3000.

|gamey
|Docker Container (Rust)
|Logic for Game Y moves and win validation. Accessible via Port 4000.

|prometheus
|Docker Container
|Scrapes metrics from the services via the internal network.

|grafana
|Docker Container
|Visualizes metrics. Mapped to Port 9091 to avoid conflict with User Service.
|===

=== Infrastructure Level 3: Data Persistence

|===
|Element |Storage Strategy |Configuration Path

|Metrics & Config
|Docker Bind Mount
|./users/monitoring/prometheus mapped to /etc/prometheus

|Dashboards
|Docker Bind Mount
|./users/monitoring/grafana/provisioning for automated setup.

|User Data
|Docker Volume
|Persistence for user profiles and match history in MongoDB.
|===

=== Network and Connectivity

- Port 80: Entry point for the web application.

- Port 3000: API endpoint for User/History management (used by Webapp and Bots).

- Port 4000: API endpoint for Game Engine moves (YEN notation).

- Port 9090/9091: Administrative access for system observability (Prometheus and Grafana respectively).